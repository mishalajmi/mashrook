package sa.elm.mashrook.notifications;

/**
 * Represents the type of notification channel.
 * Each type corresponds to a specific delivery mechanism.
 */
public enum NotificationType {
    EMAIL,
    SMS,
    PUSH
}
